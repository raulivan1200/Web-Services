info:
  name: Events - Post - 405
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/events"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Unsupported method returns 405 Method Not Allowed", function() {
          expect(res.getStatus()).to.equal(405);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
